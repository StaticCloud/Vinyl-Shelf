import { LoadingSpinner, LoadingWrapper } from './styled-loading';

const Loading = () => {
    return (
        <LoadingWrapper>
            <LoadingSpinner></LoadingSpinner>
        </LoadingWrapper>
    )
}

export default Loading;