import styled from "styled-components";
import { LoadingSpinner } from "./LoadingSpinner";

export const LoadingMini = styled(LoadingSpinner)`
    min-width: 30px;
    min-height: 30px;
    border-radius: 50%;
    background-size: 2rem;
`;