import styled from "styled-components";

const StyledNav = styled.nav`
    position: fixed;
    display: flex;
    width: 100svw;
    height: 60px;
    bottom: 0;
    justify-content: center;
`;

export const Nav = () => {
    return (
        <StyledNav>
        </StyledNav>
    );
}